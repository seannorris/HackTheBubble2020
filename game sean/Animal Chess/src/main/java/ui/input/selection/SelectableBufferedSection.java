package ui.input.selection;

import ui.drawing.BufferedSection;

/**
 * Represents a selectable buffered section (i.e. implements SelectableSection and BufferedSection).
 */
public interface SelectableBufferedSection extends SelectableSection, BufferedSection {}