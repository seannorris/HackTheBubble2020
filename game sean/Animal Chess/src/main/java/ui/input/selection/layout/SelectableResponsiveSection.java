package ui.input.selection.layout;

import ui.drawing.layout.ResponsiveSection;
import ui.input.selection.SelectableSection;

/**
 * Represents a selectable responsive section (i.e. implements SelectableSection and ResponsiveSection).
 */
public interface SelectableResponsiveSection extends SelectableSection, ResponsiveSection {}
