The inheritance overview diagram shows the hierarchy between classes.

The full diagram shows all dependencies and was too big to export as an image from intellij.
To solve this I used the XPS printer utility and specified a paper size of 100m x 100m.
Because of this the pdf generated is extremely large.